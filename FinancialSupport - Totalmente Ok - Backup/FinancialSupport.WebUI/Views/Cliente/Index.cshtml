@model List<FinancialSupport.Application.DTOs.UsuarioDTO>

@{
    ViewData["Title"] = "Operação dos clientes";
}

<style>
    .modal-lg {
        max-width: 80%;
    }
</style>

<div>
    <h2>@ViewData["Title"]</h2>
</div>
<hr />
<div class="row">
    <div class="col-md-12">
        <div>
            <div class="pull-left">
                <a asp-action="Create" class="btn btn-primary">
                    <span title="Create" class="fas fa-plus"></span>Novo Cliente
                </a>
            </div>
        </div>
    </div>
</div>
<br />
<div class="panel panel-default">
    <table class="table table-striped text-center">
        <thead>
            <tr>
                <th>
                    Nome
                </th>
                <th>
                    Limite
                </th>
                <th>
                    Limite Disponível
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Limite)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LimiteDisponivel)
                    </td>
                    <td>
                        <a asp-action="Pagamento" asp-route-id="@item.Id" title="Pagamento" class="btn btn-success">
                            <span class="fas fa-edit">Pagamento</span>
                        </a>
                        <a asp-action="Details" asp-route-id="@item.Id" title="Details" class="btn btn-warning">
                            <span class="fas fa-search">Detalhamento</span>
                        </a>
                        <a asp-action="Novo" asp-route-id="@item.Id" title="Novo" class="btn btn-danger">
                            <span class="fas fa-edit">Novo empréstimo</span>
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
